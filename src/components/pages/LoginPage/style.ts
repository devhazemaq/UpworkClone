import { StyledFlex } from "@/styles/common";
import styled from "styled-components";

export const StyleMainLoginPage = styled(StyledFlex)`
  height: 100vh;
  flex-direction: column;
  justify-content: space-between;
`;
